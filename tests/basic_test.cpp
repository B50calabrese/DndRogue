#include <gtest/gtest.h>

/**
 * @brief Basic test case to ensure the testing framework is working.
 */
TEST(BasicTest, Initialization) {
  EXPECT_TRUE(true);
}

/**
 * @brief Another basic test case.
 */
TEST(BasicTest, MathWorks) {
  EXPECT_EQ(2 + 2, 4);
}
